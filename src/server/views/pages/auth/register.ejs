<%- include('../../partials/header'); %>

<div class="govuk-width-container">
    <% if(locals?.errors?.global) { %>
        <div class="govuk-error-summary" data-module="govuk-error-summary">
            <div role="alert">
                <div class="govuk-error-summary__body">
                    <ul class="govuk-list govuk-error-summary__list">
                        <%= locals?.errors?.global %>
                    </ul>
                </div>
            </div>
        </div>
    <% } %>
    <main class="govuk-main-wrapper " id="main-content" role="main">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">
                <h1 class="govuk-heading-l">Create an account</h1>
                <p>You will be able to access this service’s API. If you are a UK regulator, you will also be able to upload documents to this service.</p>

                <form method="post">
                    <div class="govuk-form-group <% if(locals?.errors?.email) { %>govuk-form-group--error<% } %>">
                        <h2 class="govuk-label-wrapper">
                            <label class="govuk-label govuk-label--s" for="email">Email</label>
                        </h2>
                        <div id="search-title-hint" class="govuk-hint govuk-!-font-size-16">Please use your work or organisational email</div>
                        <% if(locals?.errors?.email) { %>
                            <p id="email-error" class="govuk-error-message">
                                <%= locals?.errors?.email %>
                            </p>
                        <% } %>
                        <input class="govuk-input govuk-!-width-two-thirds" id="email" name="email" type="text" <% if(locals?.values?.email) { %>value="<%= locals?.errors?.email %>" <% } %> />
                    </div>
                    <div class="govuk-form-group <% if(locals?.errors?.password) { %>govuk-form-group--error<% } %>">
                        <h2 class="govuk-label-wrapper">
                            <label class="govuk-label govuk-label--s" for="password">Password</label>
                        </h2>
                        <div id="search-title-hint" class="govuk-hint govuk-!-font-size-16">
                            Password must:
                            <ul>
                                <li>be at least 8 characters long</li>
                                <li>contain at least 1 number</li>
                                <li>contain at least 1 special character</li>
                                <li>contain at least 1 uppercase letter</li>
                                <li>contain at least 1 lowercase letter</li>
                            </ul>
                        </div>
                        <% if(locals?.errors?.password) { %>
                            <p id="password-error" class="govuk-error-message">
                                <%= locals?.errors?.password %>
                            </p>
                        <% } %>
                        <input class="govuk-input govuk-!-width-one-half" id="password" name="password" type="password" data-module="password-reveal" <% if(locals?.values?.password) { %>value="<%= locals?.values?.password %>" <% } %> />
                    </div>

                    <button type="submit" class="govuk-button govuk-!-margin-bottom-0" data-module="govuk-button">
                        Create account
                    </button>
                </form>
            </div>
        </div>
    </main>
</div>

<%- include('../../partials/footer'); %>
